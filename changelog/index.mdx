---
title: "Changelog"
description: "Release notes and updates for the Alvys Public API, webhooks, MCP server, and integrations, with new entries added when surfaces change."
---

Release notes for the Alvys Public API. The self-healing documentation agent prepends new entries at the top of this page when API or MCP surfaces change.

---

## 2026-07-13 — Carrier payment trip status fix (ACC-1677)

Released with the [Week of July 6–13 platform fixes](https://app.notion.com/p/39ca5b0d388a812e9db2e30d7b9526aa).

### `POST /api/p/v{version}/invoices/carrier-payments`

- **Removed** `MarkAsPaid` from the request body. The field only existed to force a `Paid` trip status, which Alvys does not use. Existing callers that still send `markAsPaid` are unaffected — the field is ignored.
- **Fixed** trip status in the response: when recorded payments fully cover the carrier payable, the trip now transitions to **`Completed`** instead of **`Paid`**. Partial payments leave the trip status unchanged.
- The response `Status` field reflects the trip's current status after the payment is applied.

### MCP: `invoices_record_carrier_payment`

- **Removed** the `markAsPaid` parameter (same behavior as the Public API change above).

<Note>
  Historical trips may still carry a `Paid` status from before this fix. A one-time data backfill is planned separately.
</Note>

---

## 2026-07-13 — Mintlify POC bootstrap

Initial Alvys-branded Mintlify site scaffold:

- Documentation, API Reference, AI & Agents, and Changelog navigation
- Getting Started, Authentication, Versioning, and Base URL guides
- OpenAPI-driven API reference from `openapi/alvys.json`
- MCP tool catalog under `ai-agents/`

_Full historical changelog entries will be ported incrementally from `alvysio/api-docs`._
